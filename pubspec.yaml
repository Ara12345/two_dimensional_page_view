name: two_dimensional_page_view
description: "A Flutter package for Two Dimensional Page View."
version: 0.0.1
homepage: https://github.com/Ara12345/two_dimensional_page_view

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  plugin:
    platforms:
      android:
      ios:
      linux:
      macos:
      web:
      windows:
